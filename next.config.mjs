/** @type {import('next').NextConfig} */
const nextConfig = {
	// Add any specific Next.js configurations here if needed
	// For example, if you want to use the 'src' directory, you'd configure it here.
	// However, the default setup already supports `src/app`.
};

export default nextConfig;
